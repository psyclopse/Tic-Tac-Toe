# Backend (Express) — used locally and on Render
PORT=3001
NODE_ENV=development

# Comma-separated allowed frontend origins for CORS (no trailing slashes)
# Example production: https://your-app.vercel.app,https://your-app-git-main.vercel.app
FRONTEND_URL=http://localhost:3000
